{
  "name": "social",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "dev": "npm run sync-data && vite",
    "build": "npm run sync-data && tsc && vite build",
    "export": "npm run build && node scripts/export-instagram.mjs",
    "export:instagram": "npm run export",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/d3": "^7.4.3",
    "playwright": "^1.48.0",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
